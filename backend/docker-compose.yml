version: '3.8'

services:
  mongodb:
    image: mongo:latest
    container_name: berakah_mongodb
    ports:
      - '27018:27017'
    volumes:
      - mongodb_data:/data/db
    restart: unless-stopped

volumes:
  mongodb_data:
